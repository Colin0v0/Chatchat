# Chatchat 开发文档

更新时间：2026-04-09

本文档描述当前代码已经落地的真实能力、目录职责、关键链路和开发约定。它是给开发者看的，不是产品宣传文档。

## 1. 当前产品形态

Chatchat 现在是一个“带登录、多模型、多模态、检索、记忆”的聊天工作台。

当前已经落地的功能：

- 账号密码登录
- Cookie Session 鉴权
- 多用户隔离的会话与记忆
- 多模型聊天
- reasoning / thinking 流式展示与持久化
- 多用户隔离的知识库 RAG 检索
- Web Search
- 图片和文件上传
- 本地附件解析链路
- 原生多模态上游上传链路
- 语音录音与转写
- Memory 面板、候选记忆、记忆文档

当前明确不做的事情：

- 不开放注册
- 不把业务逻辑继续堆进 `main.py`
- 不维护大量隐式 fallback
- 不为同一能力维护三套并行实现

## 2. 根目录与职责

```text
Chatchat/
  backend/
  frontend/
  storage/
  docker-compose.yml
  README.md
  开发文档.md
```

### 2.1 `backend/`

- FastAPI 应用
- SQLAlchemy ORM
- 聊天、检索、memory、多模态、鉴权

### 2.2 `frontend/`

- React 19 + Vite + TypeScript
- 单页应用
- 负责聊天 UI、登录页、侧边栏、记忆面板、流式展示

### 2.3 `storage/`

- SQLite 数据库
- 媒体附件
- 用户知识库文件

## 3. 后端目录结构

```text
backend/app/
  api/
  auth/
  audio/
  chat/
  core/
  llm/
  memory/
  multimodal/
  retrieval/
  storage/
  main.py
  schemas.py
```

### 3.1 `api/`

路由层，只做：

- 入参校验
- 当前用户鉴权
- 调用领域服务
- 返回 response model

当前主要接口：

- `api/auth.py`
- `api/chat.py`
- `api/conversations.py`
- `api/memories.py`
- `api/models.py`
- `api/knowledge.py`
- `api/audio.py`

### 3.2 `auth/`

负责：

- 用户查找
- 密码校验
- Session 创建与失效
- Cookie 写入/清理

当前特点：

- 无注册接口
- 账号通过脚本或数据库创建
- 登录失败分为：
  - `user_not_found`
  - `invalid_password`

### 3.3 `chat/`

这是聊天主链路域。

当前核心文件：

- `workflow.py`
- `streamer.py`
- `history.py`
- `context.py`
- `prompt_builder.py`
- `model_queue.py`
- `state.py`

职责：

- 创建用户消息
- 保存附件元数据
- 裁剪历史上下文
- 准备注入给模型的 message history
- 生成 NDJSON 流式响应
- 保存 assistant message
- 触发 memory 刷新

### 3.4 `core/`

基础设施层。

当前核心文件：

- `config.py`
- `logging.py`
- `http.py`

其中：

- `config.py`
  - 环境变量与配置项
- `http.py`
  - 共享 `httpx.AsyncClient`
  - 上游请求闸门
- `logging.py`
  - 日志初始化

### 3.5 `llm/`

模型接入层。

当前核心文件：

- `catalog.py`
- `registry.py`
- `service.py`
- `openai_client.py`
- `ollama_client.py`
- `thinking.py`

职责：

- 解析模型目录
- 归一化模型 id
- 返回前端模型能力
- 调用上游聊天接口
- 统一 thinking / reasoning 行为

### 3.6 `memory/`

负责：

- memory 抽取
- memory 规范化
- memory 存储
- memory 文档构建
- memory prompt 注入

当前 memory 类型：

- `global`
- `conversation`
- `working`

当前 memory 状态：

- `active`
- `candidate`
- `archived`

### 3.7 `multimodal/`

负责：

- 图片处理
- OCR
- 文档解析
- 附件上下文聚合

当前关键文件：

- `image.py`
- `ocr.py`
- `vision.py`
- `file_parser.py`
- `attachment.py`

### 3.8 `retrieval/`

这是统一检索层。

当前包括：

- `plan.py`
- `service.py`
- `strategy.py`
- `file_context.py`
- `rag/`
- `websearch/`

设计原则：

- RAG 与 Web Search 都属于“上下文获取”
- 不再分别维护两套上层编排

### 3.9 `storage/`

负责：

- 数据库引擎
- ORM model
- 媒体落盘
- 数据访问辅助函数

当前关键文件：

- `database.py`
- `models.py`
- `media.py`
- `access.py`

## 4. 前端目录结构

```text
frontend/src/
  app/
  components/
  lib/
  types.ts
  App.tsx
```

### 4.1 `App.tsx`

顶层装配：

- 路由归一化
- 登录态切换
- 未授权跳转
- Workspace 布局

### 4.2 `app/`

这是前端业务 hook 层。

当前关键文件：

- `useAuthSession.ts`
- `useChatApp.ts`
- `useConversationStreams.ts`
- `useComposerAttachments.ts`
- `useMemoryManager.ts`
- `useLatestRequestGuard.ts`
- `chatSessionUtils.ts`

职责拆分：

- `useAuthSession.ts`
  - 登录、登出、恢复 session

- `useChatApp.ts`
  - 页面级状态中心
  - 会话列表、当前会话、输入草稿、模型切换、发送/重试/停止

- `useConversationStreams.ts`
  - 流式事件消费
  - 维护 draft assistant message
  - 处理中断、恢复输入、流式 stage

- `useComposerAttachments.ts`
  - 输入区附件草稿

- `useMemoryManager.ts`
  - 记忆面板的数据加载与编辑

- `useLatestRequestGuard.ts`
  - 前端“只接收最新请求结果”的公共保护逻辑

### 4.3 `components/`

主要是展示层组件：

- 登录页
- 侧边栏
- Header
- 对话区
- 输入框
- ThinkingPanel
- MarkdownMessage
- SettingsDialog

### 4.4 `lib/api.ts`

前端 API 封装层。

负责：

- 普通 JSON 请求
- 带 Cookie 的 `fetch`
- NDJSON 流式解析
- 音频上传
- 统一错误转换

## 5. 当前数据模型

当前核心表：

- `users`
- `user_sessions`
- `conversations`
- `messages`
- `message_attachments`
- `knowledge_documents`
- `knowledge_chunks`
- `memory_items`
- `memory_documents`

### 5.1 `messages`

重要字段：

- `content`
- `reasoning_content`
- `attachment_context`
- `image_context`
- `sources_json`
- `context_json`
- `feedback_value`

说明：

- `reasoning_content`
  - 保存 reasoning 面板内容
- `attachment_context`
  - 当前统一附件上下文缓存字段
- `image_context`
  - 保留兼容字段

### 5.2 `message_attachments`

重要字段：

- `kind`
- `original_name`
- `mime_type`
- `relative_path`
- `upstream_file_id`
- `extension`
- `position`

其中：

- `kind` 当前只用：
  - `image`
  - `file`

- `upstream_file_id`
  - 用于 `native_multimodal=true` 的上游文件上传缓存

### 5.3 `memory_items`

Memory 维度：

- scope
- kind
- status
- source_type
- write_policy
- pinned
- active

当前自动写入策略：

- 显式“记住”类语义可直接写入 active memory
- 默认自动抽取写成：
  - `working active`
  - 或 `conversation candidate`

### 5.4 `memory_documents`

当前文档类型：

- `user_profile`
- `workspace_profile`
- `conversation_brief`

用途：

- 作为更稳定、压缩后的 memory 注入载体

## 6. 聊天主链路

当前一轮聊天大致流程如下：

1. 前端输入文本/附件
2. `frontend/lib/api.ts` 组装 `FormData`
3. 请求 `POST /api/chat/stream`
4. 后端 `chat/workflow.py` 创建 conversation/user message
5. 保存附件到 `message_attachments`
6. `chat/streamer.py` 先输出 `meta`
7. `MessageHistoryService` 准备历史消息
8. 如需要，本地附件上下文先补齐
9. `RetrievalService` 按 `retrieval_mode` 构造上下文
10. `MemoryService` 生成 memory prompt
11. `llm/service.py` 调用实际模型
12. 流式输出：
    - `status`
    - `reasoning`
    - `token`
    - `sources`
    - `context`
    - `done`
13. 保存 assistant message
14. 后台异步调度 memory refresh

## 7. Thinking / Reasoning 逻辑

当前 thinking 逻辑是“后端标准化，前端展示”。

### 7.1 后端处理方式

后端会兼容两种来源：

- 上游直接返回 reasoning 字段
- 正文里混入 `<think>...</think>`

处理结果：

- 流式事件统一发 `reasoning`
- 正文只发 answer token
- `done` 前保存完整 `reasoning_content`

### 7.2 前端处理方式

前端：

- 逐步展示 reasoning 面板
- 正式 answer 开始输出前，reasoning 流状态结束
- reasoning 内容仍保留，可展开
- 刷新历史会从 message.reasoning 恢复

## 8. 检索架构

### 8.1 当前模式

前端只传显式模式：

- `none`
- `rag`
- `web`

后端不再依赖一套复杂 planner 来决定“要不要查”。

### 8.2 `RetrievalService`

负责：

- 生成 `RetrievalPlan`
- 并行执行 RAG / Web / 文件上下文
- 合并来源
- 对不同来源打分
- 生成注入模型的 system context

### 8.3 RAG

当前 RAG 已经改成“每个用户自己的知识库”：

- 只接受用户上传的 `.md`
- 文件按用户隔离存储在 `storage/knowledge/`
- 支持批量上传、批量删除
- 上传时只落盘和入库，不立即切分
- 点击“更新知识库”后才批量切分、embedding、写入 chunk
- 默认 embedding 模型为 `qwen3-embedding:0.6b`
- 默认 rerank 模型为 `dengcao/Qwen3-Reranker-0.6B:Q8_0`
- 检索前会做一层 RAG 查询重写，把“那一段 / 上面那个方案”改成更完整的知识库查询
- 分块后写入 `knowledge_chunks`
- 检索时严格按 `user_id` 过滤
- 不再维护全局笔记目录扫描和 `index.json`

关键文件：

- `knowledge/service.py`
- `retrieval/query_rewrite.py`
- `retrieval/rag/chunking.py`
- `retrieval/rag/retriever.py`

### 8.4 Web Search

当前 Web Search 走 Tavily。

关键文件：

- `retrieval/websearch/service.py`
- `retrieval/websearch/planner.py`
- `retrieval/websearch/translator.py`
- `retrieval/websearch/providers/tavily.py`

当前已加：

- 查询规划
- 翻译
- 结果去重/过滤/重排
- 共享连接池
- 请求并发闸门

## 9. 多模态与附件链路

### 9.1 总原则

当前只有一个关键模型能力开关：

- `native_multimodal`

它定义在 [backend/model_catalog.json](E:/VScodeproject/Chatchat/backend/model_catalog.json)。

### 9.2 `native_multimodal=false`

走本地链路：

- 图片：
  - 本地视觉模型
  - OCR
- 文件：
  - `pypdf`
  - `python-docx`
  - `openpyxl`
  - 文本/CSV/JSON 等本地解析

产物：

- `attachment_context`
- 写回 `messages`

### 9.3 `native_multimodal=true`

走原生上游链路：

- 直接上传到上游 `/v1/files`
- 消息里传 `input_file`
- 不走本地 OCR
- 不走本地文件解析
- 失败直接报错，不兜底

### 9.4 当前链路文件

- `chat/history.py`
- `chat/upstream_files.py`
- `multimodal/attachment.py`
- `multimodal/file_parser.py`
- `multimodal/image.py`

### 9.5 当前优化点

本轮已加：

- 附件本地处理并发闸门
- 文件解析放到线程中执行
- 避免阻塞事件循环

## 10. 模型目录与能力管理

模型目录文件：

- `backend/model_catalog.json`

当前支持字段：

- `provider_ref`
- `upstream_model`
- `thinking_mode`
- `context_window`
- `native_multimodal`
- `enabled`

### 10.1 当前 provider

- `openai_default`
- `openai_local_router`
- `ollama_local`

### 10.2 当前已经启用的主要模型

以当前 `model_catalog.json` 为准，常见包括：

- `openai:deepseek-chat`
- `openai:deepseek-reasoner`
- `openai_local:claude-sonnet-4-6`
- `openai_local:claude-haiku-4-5`
- `openai_local:claude-opus-4-6`

其中当前目录里的多模态开关状态是：

- `openai_local:claude-sonnet-4-6`：`native_multimodal=true`
- `openai_local:claude-haiku-4-5`：`native_multimodal=false`
- `openai_local:claude-opus-4-6`：`native_multimodal=false`

### 10.3 当前前端模型能力字段

`/api/models` 返回给前端的 `ModelOption` 当前包括：

- `supports_thinking`
- `supports_thinking_trace`
- `supports_attachment_upload`
- `chat_model`
- `reasoning_model`

当前 `supports_attachment_upload` 统一返回 `true`，真正的多模态分流由后端 `native_multimodal` 决定。

## 11. Memory 系统

### 11.1 当前组成

- `MemoryStore`
- `MemoryService`
- `MemoryExtractor`
- `memory_search` FTS
- `memory_documents`

### 11.2 当前 prompt 注入

Memory 注入不是直接把所有 memory 都塞给模型，而是：

- 先召回 relevant hits
- 再加载 memory documents
- 构造成 system prompt 注入

### 11.3 当前后台刷新

assistant 回答结束后会异步调度 memory refresh。

当前已经做的稳定性控制：

- 同一会话只保留最新 refresh task
- 旧任务会取消
- 有 `memory_refresh_max_concurrency` 并发上限

## 11.4 RAG 查询重写

RAG 查询重写是知识库检索前的一层文本预处理，不是新的检索模式。

作用：

- 结合最近几轮对话
- 把代词、指代和省略信息补全
- 输出一个更适合知识库召回的独立查询

当前配置项：

- `RAG_QUERY_REWRITE_ENABLED`
- `RAG_QUERY_REWRITE_MODEL`
- `RAG_QUERY_REWRITE_HISTORY_MESSAGES`

当前行为：

- 只在 `rag` 模式启用
- 只影响知识库检索 query
- 原始用户问题不会被替换
- debug context 里会记录是否应用了重写

## 12. 鉴权与用户链路

### 12.1 当前接口

- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/auth/session`

### 12.2 当前 memory 相关接口

主接口：

- `GET /api/memories`
- `POST /api/memories/items`
- `PATCH /api/memories/items/{memory_id}`
- `DELETE /api/memories/items/{memory_id}`
- `POST /api/memories/{memory_id}/promote`
- `POST /api/memories/{memory_id}/dismiss`

### 12.3 当前知识库接口

- `GET /api/knowledge/status`
- `GET /api/knowledge/documents`
- `POST /api/knowledge/documents`
- `POST /api/knowledge/documents/batch`
- `POST /api/knowledge/documents/{document_id}/reindex`
- `POST /api/knowledge/reindex`
- `DELETE /api/knowledge/documents/{document_id}`
- `POST /api/knowledge/documents/delete`

### 12.3 当前前端行为

- 应用启动时先尝试恢复 session
- 未登录自动进入 `/login`
- `401` 会触发统一未授权处理

### 12.4 当前账号创建方式

使用脚本：

```powershell
cd backend
python scripts/create_user.py --username alice --password secret123
```

如果账号已存在要重置密码：

```powershell
cd backend
python scripts/create_user.py --username alice --password newpass --reset-password
```

## 13. 共享 HTTP 连接池与并发控制

这是当前后端新加的重要基础设施。

### 13.1 位置

- `backend/app/core/http.py`

### 13.2 功能

- 共享 `httpx.AsyncClient`
- 避免每次请求都新建连接池
- 用请求闸门限制：
  - `openai`
  - `openai_local`
  - `ollama`
  - `web_search`

### 13.3 当前相关配置

- `HTTP_POOL_MAX_CONNECTIONS`
- `HTTP_POOL_MAX_KEEPALIVE_CONNECTIONS`
- `OPENAI_HTTP_MAX_CONCURRENCY`
- `OPENAI_LOCAL_HTTP_MAX_CONCURRENCY`
- `OLLAMA_HTTP_MAX_CONCURRENCY`
- `WEB_SEARCH_HTTP_MAX_CONCURRENCY`

## 14. 会话列表与前端状态保护

### 14.1 会话列表

以前会话列表为了拿最后一句 preview，会把整段消息历史都加载出来。

现在：

- 查询逻辑收口到 `storage/access.py`
- 只查摘要所需字段
- API 层不再做复杂查询拼接

### 14.2 前端最新请求保护

当前前端很多地方都有“只接受最后一次异步请求结果”的需求。

现在统一由：

- `frontend/src/app/useLatestRequestGuard.ts`

复用到：

- `useAuthSession.ts`
- `useChatApp.ts`
- `useMemoryManager.ts`

这样避免：

- 旧请求覆盖新状态
- 切会话后页面回跳
- memory 面板打开/关闭时状态错乱

## 15. 环境配置与运行

### 15.1 分层 env

后端读取顺序：

1. `backend/.env`
2. `backend/.env.<name>`，由 `CHATCHAT_ENV` 决定
3. `CHATCHAT_ENV_FILE` 指定的附加 env 文件

### 15.2 本地开发

后端：

```powershell
cd backend
python app.py --reload
```

前端：

```powershell
cd frontend
npm install
npm run dev
```

默认开发端口约定：

- 前端：`5200`
- 后端：`8050`
- Docker 部署前端：`3300`
- Docker 部署后端：`8000`

### 15.3 Docker

```bash
docker compose up -d --build
docker compose down
```

### 15.4 WSL2 / SSH Tunnel

如果模型路由不和后端在同一网络环境：

- Windows 跑模型路由，WSL2 跑后端
- 远端 SSH 转发模型路由

都要保证这两个地址对当前运行时可达：

- `OPENAI_LOCAL_BASE_URL`
- `OPENAI_LOCAL_UPSTREAM_SERVICE_BASE_URL`

## 16. 测试

当前后端已有测试覆盖：

- OpenAI stream 解析
- model queue
- message history
- native multimodal routing
- memory refresh 调度
- shared http client
- storage access summary query
- web search service

运行：

```powershell
$env:PYTHONPATH='E:\VScodeproject\Chatchat\backend'
python -m pytest backend\tests
```

前端构建验证：

```powershell
cd frontend
npm run build
```

## 17. 开发约定

继续开发时默认遵循：

1. 领域逻辑往对应目录收，不往 `main.py` 和路由层回堆
2. 有重复状态保护时，优先抽 hook / helper
3. 有重复查询时，优先下沉到 `storage/access.py`
4. 多模态能力判断只认 `native_multimodal`
5. 不随意新增 fallback 分支
6. 前后端协议字段名改动时一次收干净

## 18. 当前文档与代码同步情况

这份文档已按当前代码同步了这些变化：

- 登录与多用户
- memory 系统
- `native_multimodal` 双链路
- reasoning 持久化
- 共享 HTTP 连接池
- 上游请求限流
- 会话摘要查询下沉
- 前端最新请求保护

如果后面继续改链路，请至少同步：

- `README.md`
- `开发文档.md`
- `backend/model_catalog.json`
- 相关 `.env.*.example`
