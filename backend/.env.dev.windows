APP_ENV=dev.windows

# Windows 本地开发
DATABASE_URL=sqlite:///./storage/app.db
MEDIA_ROOT=./storage/media
MODEL_CATALOG_PATH=./model_catalog.json
MODEL_CATALOG_STRICT=true

# 推荐把 SSH 隧道开在 Windows 本机：
OPENAI_LOCAL_BASE_URL=http://127.0.0.1:18000/v1
OPENAI_LOCAL_UPSTREAM_SERVICE_BASE_URL=http://127.0.0.1:18000/v1
OPENAI_LOCAL_API_KEY=sk-local
OPENAI_LOCAL_STREAM=true

# Ollama 本地配置（如果使用 Ollama 模型）
OLLAMA_BASE_URL=http://127.0.0.1:11434

# 默认模型
DEFAULT_PROVIDER=openai_local
DEFAULT_MODEL=openai_local:claude-opus-4-6

# 知识库存储
KNOWLEDGE_STORAGE_ROOT=./storage/knowledge
KNOWLEDGE_EMBEDDING_MODEL=qwen3-embedding:0.6b
KNOWLEDGE_RERANK_MODEL=openai_local:claude-sonnet-4-6
IMAGE_VISION_MODEL=F:/AI/Models/Florence-2-base-ft

# 本地开发可先关掉音频，避免 funasr 没装导致服务起不来
AUDIO_TRANSCRIPTION_ENABLED=true
AUDIO_TRANSCRIPTION_EAGER_LOAD=true

# Web search 配置
WEB_SEARCH_TRANSLATION_MODEL=openai_local:claude-haiku-4-5
RAG_QUERY_REWRITE_ENABLED=true
RAG_QUERY_REWRITE_MODEL=openai_local:claude-haiku-4-5
RAG_QUERY_REWRITE_HISTORY_MESSAGES=6
